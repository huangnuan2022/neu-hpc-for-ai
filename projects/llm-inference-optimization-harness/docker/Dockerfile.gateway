FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml ./
COPY serving ./serving
RUN pip install --no-cache-dir '.[telemetry]'

EXPOSE 8000
CMD ["llm-serving-gateway", "--host", "0.0.0.0", "--port", "8000"]
